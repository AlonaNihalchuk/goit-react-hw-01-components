// import PropTypes from 'prop-types';

// function StatisticalSection({ title, children }) {
//   return (
//     <section>
//       {title && <h2>{title}</h2>}
//       {children}
//     </section>
//   );
// }

// StatisticalSection.propType = {
//   title: PropTypes.string,
//   children: PropTypes.node,
// };
// export default StatisticalSection;
